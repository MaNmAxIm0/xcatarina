"use client";
import { FormEvent,useState } from "react";
export function AdminUploader({email}:{email:string}){const[busy,setBusy]=useState(false);const[message,setMessage]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=event.currentTarget;setBusy(true);setMessage("A enviar o vídeo…");const response=await fetch("/api/videos",{method:"POST",body:new FormData(form)});const data=await response.json() as{error?:string};setBusy(false);if(!response.ok){setMessage(data.error||"Não foi possível publicar.");return}form.reset();setMessage("Vídeo publicado com sucesso.")}return <main className="admin-shell"><a href="/" className="back">← Voltar ao site público</a><section className="admin-card"><span className="admin-kicker">ÁREA PRIVADA</span><h1>Publicar vídeo</h1><p>Sessão: {email}. Apenas o endereço autorizado consegue concluir o envio.</p><form onSubmit={submit}><label>Título<input name="title" required maxLength={100}/></label><label>Descrição<textarea name="description" rows={3} maxLength={300}/></label><div className="admin-grid"><label>Categoria<select name="category" required defaultValue="lego"><option value="lego">LEGO</option><option value="art">Arte / Pintura</option></select></label><label>Duração<input name="duration" placeholder="00:15" maxLength={10}/></label></div><label>Vídeo<input name="video" type="file" accept="video/*" required/></label><label className="check"><input name="featured" value="true" type="checkbox"/> Mostrar em destaque</label><button disabled={busy}>{busy?"A publicar…":"Publicar no site"}</button><output>{message}</output></form></section></main>}
